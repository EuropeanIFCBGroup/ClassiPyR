# Save file index to disk cache

Writes the file index data to a JSON cache file for fast startup.

## Usage

``` r
save_file_index(data)
```

## Arguments

- data:

  List containing scan results (sample names, path maps, etc.)

## Value

NULL (called for side effects)
